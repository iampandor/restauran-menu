<%= form_with(model: ingredient_x_dish, local: true) do |form| %>
  <% if ingredient_x_dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient_x_dish.errors.count, "error") %> prohibited this ingredient_x_dish from being saved:</h2>

      <ul>
        <% ingredient_x_dish.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ingredient_id %>
    <%= form.number_field :ingredient_id %>
  </div>

  <div class="field">
    <%= form.label :dish_id %>
    <%= form.number_field :dish_id %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field :weight %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
