<%= form_with(model: ingredientx_tag, local: true) do |form| %>
  <% if ingredientx_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredientx_tag.errors.count, "error") %> prohibited this ingredientx_tag from being saved:</h2>

      <ul>
        <% ingredientx_tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ingredient_id %>
    <%= form.number_field :ingredient_id %>
  </div>

  <div class="field">
    <%= form.label :ingredient_tag_id %>
    <%= form.number_field :ingredient_tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
